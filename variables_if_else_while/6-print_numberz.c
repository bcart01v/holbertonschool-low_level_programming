#include <stdio.h>

/**
 * main - Prints numbers of base 10
 *
 * Return: Should return 0 pending no errors
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}

	putchar('\n');

	return (0);
}
